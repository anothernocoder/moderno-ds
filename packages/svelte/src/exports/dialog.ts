/**
 * Dialog — re-exported from `@ark-ui/svelte` (portal + focus trap + SSR-stable
 * ids). Composed as `Root > Trigger + Portal(> Backdrop + Positioner > Content)`.
 */
export { Dialog } from "@ark-ui/svelte";
