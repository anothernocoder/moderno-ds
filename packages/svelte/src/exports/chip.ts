/**
 * Chip — a CSS-only removable token (no Ark machine); it reports the press
 * through `onRemove`. Same recipe and `components.css` rules as every other
 * binding.
 */
export { default as Chip } from "../Chip.svelte";
export type { ChipVariant, ChipSize } from "@moderno-ui/core";
