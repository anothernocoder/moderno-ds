/**
 * Badge — a CSS-only status label (no Ark machine). Same recipe and
 * `components.css` rules as every other binding.
 */
export { default as Badge } from "../Badge.svelte";
export type { BadgeVariant, BadgeSize } from "@moderno-ui/core";
