/**
 * AreaChart (Phase 4) — a pure SVG map over a `@moderno-ui/charts-core` model.
 * It renders the identical scaffold across frameworks; it carries zero colour
 * and paints from `--chart-*` via the data-series index in components.css.
 */
export { default as AreaChart } from "../AreaChart.svelte";
