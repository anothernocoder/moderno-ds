/**
 * Skeleton — a CSS-only loading state (no Ark machine): a muted placeholder in
 * a content shape. Same recipe and `components.css` rules as every other
 * binding.
 */
export { default as Skeleton } from "../Skeleton.svelte";
export type { SkeletonShape } from "@moderno-ui/core";
