/**
 * Indicator — a CSS-only status dot (no Ark machine). Same recipe and
 * `components.css` rules as every other binding.
 */
export { default as Indicator } from "../Indicator.svelte";
export type { IndicatorVariant, IndicatorSize } from "@moderno-ui/core";
