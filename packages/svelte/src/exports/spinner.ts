/**
 * Spinner — a CSS-only loading state (no Ark machine): a ring with a
 * screen-reader label. Same recipe and `components.css` rules as every other
 * binding.
 */
export { default as Spinner } from "../Spinner.svelte";
export type { SpinnerSize } from "@moderno-ui/core";
