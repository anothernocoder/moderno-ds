# PR #200 docs preview screenshots

Built docs (`pnpm -r build`, `apps/docs/dist`) at the two widths the ticket
workflow asks for, en and es, for the review of
https://github.com/anothernocoder/moderno-ds/pull/200.

This branch exists only to host the images so they render in the PR; it carries
no source and is safe to delete once #200 is merged.
