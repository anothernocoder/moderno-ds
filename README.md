# PR #203 docs preview screenshots

The built docs (`pnpm -r build`, `apps/docs/dist`) field page — `/en/field/` and
`/es/field/` — captured at the two widths the ticket workflow asks for, in both
colour schemes, for the review of
https://github.com/anothernocoder/moderno-ds/pull/203.

Each image is the live `Preview` island the PR adds: the three sizes (`sm`,
`md`, `lg`), the invalid state, the textarea and the disabled field.

This branch exists only to host the images so they render in the PR; it carries
no source and is safe to delete once #203 is merged. The real visual-regression
baselines are #78's job.
