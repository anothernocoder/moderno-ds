---
title: Bloques
description: Secciones de página compuestas por primitivas, copiadas a tu proyecto con la CLI y tuyas para modificar.
group: Bloques
order: 100
---

import Preview from "../../../components/Preview.astro";
import FrameworkSelect from "../../../components/FrameworkSelect.astro";
import BlocksDemo from "../../../islands/BlocksDemo.svelte";
import loginFormReact from "../../../../../../registry/blocks/login-form/react/login-form.tsx?raw";
import loginFormVue from "../../../../../../registry/blocks/login-form/vue/LoginForm.vue?raw";
import loginFormSolid from "../../../../../../registry/blocks/login-form/solid/login-form.tsx?raw";
import loginFormSvelte from "../../../../../../registry/blocks/login-form/svelte/LoginForm.svelte?raw";
import pricingReact from "../../../../../../registry/blocks/pricing/react/pricing.tsx?raw";
import pricingVue from "../../../../../../registry/blocks/pricing/vue/Pricing.vue?raw";
import pricingSolid from "../../../../../../registry/blocks/pricing/solid/pricing.tsx?raw";
import pricingSvelte from "../../../../../../registry/blocks/pricing/svelte/Pricing.svelte?raw";
import emptyStateReact from "../../../../../../registry/blocks/empty-state/react/empty-state.tsx?raw";
import emptyStateVue from "../../../../../../registry/blocks/empty-state/vue/EmptyState.vue?raw";
import emptyStateSolid from "../../../../../../registry/blocks/empty-state/solid/empty-state.tsx?raw";
import emptyStateSvelte from "../../../../../../registry/blocks/empty-state/svelte/EmptyState.svelte?raw";

Un **bloque** es una _sección_ de página compuesta por primitivas — un hero, una
tabla de precios, un formulario de acceso. Lo copias a tu proyecto con la CLI y
es tuyo desde ese momento; no es una dependencia de npm.

Un bloque nunca es dueño del viewport y no guarda estado de navegación. Responde
a su **contenedor**, no al viewport, así que el mismo bloque se ve bien en una
barra lateral, en un modal o en una página completa sin saber dónde está.

```sh
npx @moderno-ui/cli add login-form-react
```

Instalar un bloque instala también los ítems del registro que compone —
primitivas extraídas, si usa alguna — y registra cada uno con su propia versión
en `.moderno/manifest.json`, de modo que `moderno update` y `moderno diff`
siguen funcionando por ítem.

Un bloque compone solo primitivas, nunca una pantalla ni un flujo — el registro
impone esa dirección. El catálogo se va llenando a medida que se absorbe el
inventario del predecesor; cada bloque publicado tendrá su propia página en esta
sección.

## Lo que existe hoy

Tres bloques, una entrada de registro por framework cada uno. Cada Preview de
abajo lee su fuente directo de `registry/blocks/<name>/<framework>/` — para los
cuatro frameworks, intercambiable con el selector — así que lo que lees es
exactamente lo que copia la CLI, no una maqueta.

<FrameworkSelect locale="es" />

### Formulario de acceso

Compone `Field` (etiqueta, texto de ayuda, texto de error) y `Button`.

<Preview
  examples={{
    react: loginFormReact,
    vue: loginFormVue,
    solid: loginFormSolid,
    svelte: loginFormSvelte,
  }}
>
  <BlocksDemo block="login-form" client:visible />
</Preview>

### Precios

Tres planes desde un arreglo `plans` local — edítalo, el archivo ya es tuyo.

<Preview
  examples={{ react: pricingReact, vue: pricingVue, solid: pricingSolid, svelte: pricingSvelte }}
>
  <BlocksDemo block="pricing" client:visible />
</Preview>

### Estado vacío

Un marcador centrado con una sola llamada a la acción.

<Preview
  examples={{
    react: emptyStateReact,
    vue: emptyStateVue,
    solid: emptyStateSolid,
    svelte: emptyStateSvelte,
  }}
>
  <BlocksDemo block="empty-state" client:visible />
</Preview>

## El layout de sección todavía no se pinta

Esas vistas previas se ven planas, y ese es el estado honesto, no una falla de
renderizado. Las primitivas del bloque sí las pinta `components.css`, pero el
contenedor propio del bloque lleva una clase `moderno-block-*` **sin reglas en
`main`**: sin grilla, sin escala de espaciado, sin superficie de tarjeta. El
layout de sección llega con el pipeline de estilos por container queries que
describe el ADR-0005.

Mostrar el hueco es el punto: estas vistas previas las captura la suite visual,
así que cuando el pipeline aterrice las líneas base se mueven y el cambio tiene
que revisarse, no descubrirse después.
