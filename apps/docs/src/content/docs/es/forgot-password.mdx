---
title: Recuperar contraseña
description: Una pantalla de recuperación a viewport completo que pide una dirección y confirma en la misma tarjeta.
group: Pantallas
order: 113
---

import Preview from "../../../components/Preview.astro";
import Install from "../../../components/Install.astro";
import FrameworkSelect from "../../../components/FrameworkSelect.astro";
import ForFramework from "../../../components/ForFramework.astro";
import ForgotPasswordScreenDemo from "../../../islands/ForgotPasswordScreenDemo.svelte";
import forgotPasswordReact from "../../../../../../registry/screens/forgot-password/react/forgot-password.tsx?raw";
import forgotPasswordVue from "../../../../../../registry/screens/forgot-password/vue/ForgotPassword.vue?raw";
import forgotPasswordSolid from "../../../../../../registry/screens/forgot-password/solid/forgot-password.tsx?raw";
import forgotPasswordSvelte from "../../../../../../registry/screens/forgot-password/svelte/ForgotPassword.svelte?raw";

<FrameworkSelect locale="es" />

<Preview
  examples={{
    react: forgotPasswordReact,
    vue: forgotPasswordVue,
    solid: forgotPasswordSolid,
    svelte: forgotPasswordSvelte,
  }}
  layout="fill"
>
  <ForgotPasswordScreenDemo client:visible locale="es" />
</Preview>

## Instalación

<Install item="forgot-password" />

## Uso

<ForFramework fw="react">

```tsx
import { ForgotPassword } from "@/components/screens/forgot-password";
```

```tsx
<ForgotPassword
  sent={sent}
  sentTo={address}
  loading={pending}
  signInHref="/sign-in"
  onSubmit={handleSubmit}
/>
```

</ForFramework>
<ForFramework fw="vue">

```ts
import ForgotPassword from "@/components/screens/ForgotPassword.vue";
```

```vue
<ForgotPassword
  :sent="sent"
  :sent-to="address"
  :loading="pending"
  sign-in-href="/sign-in"
  @submit="handleSubmit"
/>
```

</ForFramework>
<ForFramework fw="svelte">

```ts
import ForgotPassword from "@/components/screens/ForgotPassword.svelte";
```

```svelte
<ForgotPassword
  {sent}
  sentTo={address}
  loading={pending}
  signInHref="/sign-in"
  onsubmit={handleSubmit}
/>
```

</ForFramework>
<ForFramework fw="solid">

```tsx
import { ForgotPassword } from "@/components/screens/forgot-password";
```

```tsx
<ForgotPassword
  sent={sent()}
  sentTo={address()}
  loading={pending()}
  signInHref="/sign-in"
  onSubmit={handleSubmit}
/>
```

</ForFramework>

## Referencia de API

### Props

Los callbacks son props `on…` en React y Solid, en minúsculas (`onsubmit`) en Svelte y eventos (`submit`, `navigate`) en Vue.

| Prop               | Tipo                     | Por defecto     | Descripción                                                                |
| ------------------ | ------------------------ | --------------- | -------------------------------------------------------------------------- |
| `sent`             | `boolean`                | `false`         | El enlace ya salió: la tarjeta confirma en vez de preguntar.               |
| `sentTo`           | `string`                 | `""`            | La dirección que nombra la confirmación y que reenvía «Send it again».     |
| `error`            | `string`                 | —               | No se pudo enviar el enlace: muestra la alerta del formulario.             |
| `errors`           | `Record<string, string>` | —               | Mensajes por campo; aquí solo `email`.                                     |
| `loading`          | `boolean`                | `false`         | Envío en curso: todos los controles inertes, el botón ocupado.             |
| `disabled`         | `boolean`                | `false`         | No se puede recuperar (espacio solo SSO, cuenta bloqueada).                |
| `notices`          | `AlertListItem[]`        | tres de ejemplo | Notas junto a la tarjeta. `[]` oculta el aparte.                           |
| `noticesError`     | `string`                 | —               | Las notas no cargaron; este mensaje sustituye a la lista.                  |
| `noticesLoading`   | `boolean`                | `false`         | Una región ocupada sustituye a las notas.                                  |
| `onSubmit`         | `(event) => void`        | —               | Envío nativo del formulario, tanto la petición como el reenvío.            |
| `onNavigate`       | `(to, event) => void`    | —               | Se pulsó un enlace de la pantalla: `home`, `support`, `privacy` o `terms`. |
| `onNoticeAction`   | `(id) => void`           | —               | El botón de acción de una nota, con su id.                                 |
| `onDismissNotice`  | `(id) => void`           | —               | Se descartó una nota.                                                      |
| `onDismissNotices` | `() => void`             | —               | Se descartaron todas las notas.                                            |
| `onRetryNotices`   | `() => void`             | —               | Reintentar tras `noticesError`.                                            |
| `signInHref`       | `string`                 | `"#"`           | Destino de «Sign in» en el pie de la tarjeta.                              |
| `homeHref`         | `string`                 | `"#"`           | Destino del logotipo.                                                      |
| `supportHref`      | `string`                 | `"#"`           | Destino de «Contact support».                                              |
| `privacyHref`      | `string`                 | `"#"`           | Destino del enlace de privacidad.                                          |
| `termsHref`        | `string`                 | `"#"`           | Destino del enlace de términos.                                            |

### Estados

| Estado         | Props                | Qué se renderiza                                                    |
| -------------- | -------------------- | ------------------------------------------------------------------- |
| Por defecto    | —                    | La tarjeta pidiendo una dirección, con las notas al lado o debajo.  |
| Enviado        | `sent`, `sentTo`     | La misma tarjeta, confirmando en su sitio; nada alrededor se mueve. |
| Error          | `error`              | Una alerta de formulario.                                           |
| Error de campo | `errors={{ email }}` | El campo de correo inválido, con su propio mensaje.                 |
| Cargando       | `loading`            | Controles inertes, envío con `aria-busy`.                           |
| Deshabilitado  | `disabled`           | El formulario se ve pero no se puede usar.                          |
| Notas cargando | `noticesLoading`     | Una región ocupada en lugar de las notas.                           |
| Error de notas | `noticesError`       | Una alerta en lugar de las notas.                                   |
| Vacío          | `notices={[]}`       | Sin aparte; la tarjeta queda centrada y sola.                       |

### Campos del formulario

| Nombre  | Control      | Se envía                                   |
| ------- | ------------ | ------------------------------------------ |
| `email` | Correo       | Antes de `sent`.                           |
| `email` | Campo oculto | Tras `sent`, con `sentTo` para el reenvío. |

### Tamaño

| Paso del contenedor      | Cambio                                   |
| ------------------------ | ---------------------------------------- |
| `@sm` (`--container-sm`) | La cabecera pasa a una sola fila.        |
| `@md` (`--container-md`) | El pie pasa a una sola fila.             |
| `@lg` (`--container-lg`) | Las notas se colocan junto a la tarjeta. |

### Composición

| Parte                        | Papel                                                        |
| ---------------------------- | ------------------------------------------------------------ |
| [LoginForm](/es/login-form/) | La tarjeta, en modo `forgot-password`, con `titleLevel={1}`. |
| [AlertList](/es/alert-list/) | Las notas, bajo el título «About the reset link».            |

## Ejemplos

### Enviado

<Preview
  examples={{
    react: forgotPasswordReact,
    vue: forgotPasswordVue,
    solid: forgotPasswordSolid,
    svelte: forgotPasswordSvelte,
  }}
  layout="fill"
>
  <ForgotPasswordScreenDemo client:visible locale="es" state="sent" />
</Preview>

### Error

<Preview
  examples={{
    react: forgotPasswordReact,
    vue: forgotPasswordVue,
    solid: forgotPasswordSolid,
    svelte: forgotPasswordSvelte,
  }}
  layout="fill"
>
  <ForgotPasswordScreenDemo client:visible locale="es" state="error" />
</Preview>

### Error de campo

<Preview
  examples={{
    react: forgotPasswordReact,
    vue: forgotPasswordVue,
    solid: forgotPasswordSolid,
    svelte: forgotPasswordSvelte,
  }}
  layout="fill"
>
  <ForgotPasswordScreenDemo client:visible locale="es" state="field-error" />
</Preview>

### Vacío

<Preview
  examples={{
    react: forgotPasswordReact,
    vue: forgotPasswordVue,
    solid: forgotPasswordSolid,
    svelte: forgotPasswordSvelte,
  }}
  layout="fill"
>
  <ForgotPasswordScreenDemo client:visible locale="es" state="empty" />
</Preview>

## Notas

- La confirmación dice «If … has an account, a link … is on its way» exista o no la cuenta.
- Tu `onSubmit` debe responder igual, y en el mismo tiempo, para cualquier dirección; limita su frecuencia.
- `errors.email` es solo para una dirección mal formada, nunca para una desconocida.
- La cabecera de la tarjeta es una región viva `status`, así que el paso a `sent` se anuncia.
- La tarjeta enviada lleva la dirección en un campo oculto, así que «Send it again» funciona antes de hidratar.
- Presentacional: `sent` vive en tu página, y un solo `onSubmit` atiende la petición y el reenvío.
