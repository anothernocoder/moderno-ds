---
title: Gráfico de barras
description: Un gráfico de barras en SVG puro mapeado desde charts-core; un rect por categoría por serie, el color viene de los slots --chart-* por índice.
group: Componentes
order: 70
component: BarChart
pkg: "@moderno-ui/react"
---

import Preview from "../../../components/Preview.astro";
import PropsTable from "../../../components/PropsTable.astro";
import Install from "../../../components/Install.astro";
import FrameworkSelect from "../../../components/FrameworkSelect.astro";
import BarChartDemo from "../../../examples/bar-chart/bar-chart.svelte";
import reactExample from "../../../examples/bar-chart/react.tsx?raw";
import vueExample from "../../../examples/bar-chart/vue.vue?raw";
import solidExample from "../../../examples/bar-chart/solid.tsx?raw";
import svelteExample from "../../../examples/bar-chart/bar-chart.svelte?raw";

BarChart es un mapeo determinista desde un modelo construido por
`@moderno-ui/charts-core` hacia elementos `<rect>` — una barra por categoría por
serie, posicionada sobre una escala de bandas. No incluye color propio — cada
serie se pinta con `--chart-1…5` según su índice, así se re-tematiza con el
resto del sistema.

<FrameworkSelect locale="es" />

<Preview
  examples={{ react: reactExample, vue: vueExample, solid: solidExample, svelte: svelteExample }}
>
  <BarChartDemo client:visible />
</Preview>

## Instalación

<Install pkg="@moderno-ui/react" />

## Propiedades

<PropsTable component="BarChart" />
