---
title: Blocks
description: Page sections composed of primitives, copied into your project with the CLI and yours to modify.
group: Blocks
order: 100
---

import Preview from "../../../components/Preview.astro";
import FrameworkSelect from "../../../components/FrameworkSelect.astro";
import BlocksDemo from "../../../islands/BlocksDemo.svelte";
import loginFormReact from "../../../../../../registry/blocks/login-form/react/login-form.tsx?raw";
import loginFormVue from "../../../../../../registry/blocks/login-form/vue/LoginForm.vue?raw";
import loginFormSolid from "../../../../../../registry/blocks/login-form/solid/login-form.tsx?raw";
import loginFormSvelte from "../../../../../../registry/blocks/login-form/svelte/LoginForm.svelte?raw";
import pricingReact from "../../../../../../registry/blocks/pricing/react/pricing.tsx?raw";
import pricingVue from "../../../../../../registry/blocks/pricing/vue/Pricing.vue?raw";
import pricingSolid from "../../../../../../registry/blocks/pricing/solid/pricing.tsx?raw";
import pricingSvelte from "../../../../../../registry/blocks/pricing/svelte/Pricing.svelte?raw";
import emptyStateReact from "../../../../../../registry/blocks/empty-state/react/empty-state.tsx?raw";
import emptyStateVue from "../../../../../../registry/blocks/empty-state/vue/EmptyState.vue?raw";
import emptyStateSolid from "../../../../../../registry/blocks/empty-state/solid/empty-state.tsx?raw";
import emptyStateSvelte from "../../../../../../registry/blocks/empty-state/svelte/EmptyState.svelte?raw";

A **block** is a page _section_ composed of primitives — a hero, a pricing
table, a login form. You copy it into your project with the CLI and own it from
that moment on; it is not an npm dependency.

A block never owns the viewport and carries no navigation state. It responds to
its **container**, not the viewport, so the same block looks right in a sidebar,
a modal or a full page without knowing which.

```sh
npx @moderno-ui/cli add login-form-react
```

Installing a block also installs the registry items it composes — ejected
primitives, if it uses any — and records each one under its own version in
`.moderno/manifest.json`, so `moderno update` and `moderno diff` stay per item.

A block composes only primitives, never a screen or a flow — the registry
enforces that direction. The catalog is being filled as the predecessor's
inventory is absorbed; each published block gets its own page in this section.

## What ships today

Three blocks, one registry entry per framework each. Every Preview below reads
its source straight out of `registry/blocks/<name>/<framework>/` — for all four
frameworks, switchable with the selector — so what you read is exactly what the
CLI copies, not a mockup of it.

<FrameworkSelect locale="en" />

### Login form

Composes `Field` (label, helper text, error text) and `Button`.

<Preview
  examples={{
    react: loginFormReact,
    vue: loginFormVue,
    solid: loginFormSolid,
    svelte: loginFormSvelte,
  }}
>
  <BlocksDemo block="login-form" client:visible />
</Preview>

### Pricing

Three plans from a local `plans` array — edit it, it is your file now.

<Preview
  examples={{ react: pricingReact, vue: pricingVue, solid: pricingSolid, svelte: pricingSvelte }}
>
  <BlocksDemo block="pricing" client:visible />
</Preview>

### Empty state

A centred placeholder with one call to action.

<Preview
  examples={{
    react: emptyStateReact,
    vue: emptyStateVue,
    solid: emptyStateSolid,
    svelte: emptyStateSvelte,
  }}
>
  <BlocksDemo block="empty-state" client:visible />
</Preview>

## Section layout is not painted yet

Those previews look plain, and that is the honest state rather than a rendering
fault. A block's primitives are fully painted by `components.css`, but the
block's own wrapper carries a bare `moderno-block-*` class with **no rules on
`main`** — no grid, no spacing scale, no card surface. Section layout arrives
with the container-query styling pipeline described in ADR-0005.

Showing the gap is the point: these previews are captured by the visual suite,
so when the pipeline lands, the baselines move and the change has to be reviewed
rather than noticed later.
