---
title: Button
description: The reference primitive — a plain button carrying the recipe's variant and size data-attributes, painted entirely by the shared stylesheet.
group: Components
order: 10
component: Button
pkg: "@moderno-ui/react"
agentPropsHash: "sha256:33300af545b9941d8a479991b73e6523086155e59e5c60e98a3d19c5ddea76c7"
agent:
  intent: A single click action.
  whenToUse: "Any imperative action: submit, open, delete."
  whenNotToUse:
    - case: Navigation to a URL
      use: an anchor / your router's Link, styled with the button recipe if needed
    - case: On/off state
      use: Toggle
  gotchas:
    - "variant/size are the only visual knobs; do not add className color overrides."
    - "Disabled styling comes from the recipe; pass the disabled prop, don't style it."
  theming:
    - "Colors resolve from --primary / --destructive etc.; never pass a hex."
    - "Corner radius is --radius; do not set border-radius inline."
---

import Preview from "../../../components/Preview.astro";
import PropsTable from "../../../components/PropsTable.astro";
import Install from "../../../components/Install.astro";
import ButtonDemo from "../../../islands/ButtonDemo.svelte";

Button establishes the Moderno pattern: the component holds zero colour or
radius; `data-variant`/`data-size` from the recipe key every style in
`components.css`. Switching the theme re-skins it without touching the markup.

<Preview
  code={`<Button variant="primary">Save</Button>
<Button variant="outline">Cancel</Button>`}
>
  <ButtonDemo client:visible />
</Preview>

## Installation

<Install pkg="@moderno-ui/react" />

## Props

<PropsTable component="Button" />
