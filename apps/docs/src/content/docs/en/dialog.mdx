---
title: Dialog
description: A modal surface — Ark's Dialog machine traps focus, restores it on close, locks scroll, and wires aria-labelledby/aria-describedby; components.css paints the backdrop/content from the popover token slots.
group: Components
order: 30
component: Dialog
pkg: "@moderno-ui/react"
agentPropsHash: "sha256:8be3aeb790b9e0ed748b21057972ce607ba8bb61abb7a784dabead8debe87468"
agent:
  intent: A modal surface that interrupts and traps focus until dismissed.
  whenToUse: A blocking decision or short focused task the user must resolve.
  whenNotToUse:
    - case: Side panel / non-blocking, larger content
      use: Drawer
    - case: Transient, non-modal hint anchored to a trigger
      use: Popover
  gotchas:
    - Focus trap + scroll lock are handled by Ark; do not reimplement.
    - Always render a title part for the accessible name.
  theming:
    - content uses --popover surface; backdrop opacity is part-styled, not inline.
---

import Preview from "../../../components/Preview.astro";
import PropsTable from "../../../components/PropsTable.astro";
import Install from "../../../components/Install.astro";
import DialogDemo from "../../../islands/DialogDemo.svelte";

Dialog composes as `Root > Trigger + Portal(> Backdrop + Positioner > Content)`.
Ark drives focus trap, focus restore, scroll lock, and the `aria-labelledby`/
`aria-describedby` wiring. An SSR render emits only the trigger; after
hydration the content stays in the DOM between openings and Ark takes it out of
the layout with the `hidden` attribute, which `components.css` honours for every
part. No CVA variants — the surface comes straight from the `--popover` /
`--popover-foreground` token slots via `components.css`.

<Preview
  code={`<Dialog.Root>
  <Dialog.Trigger>Open</Dialog.Trigger>
  <Portal>
    <Dialog.Backdrop />
    <Dialog.Positioner>
      <Dialog.Content>
        <Dialog.Title>Delete item</Dialog.Title>
        <Dialog.Description>This can't be undone.</Dialog.Description>
      </Dialog.Content>
    </Dialog.Positioner>
  </Portal>
</Dialog.Root>`}
>
  <DialogDemo client:visible />
</Preview>

## Installation

<Install pkg="@moderno-ui/react" />

## Props

<PropsTable component="Dialog" />
