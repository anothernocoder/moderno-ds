---
title: Login form
description: The credential card in its three modes — sign in (email, password, remember-me), sign up (name, email, a new password, consent) or forgot password (the address alone, then the confirmation) — composed from Card, Field, Checkbox, Button and Alert, sized by its container rather than the viewport.
group: Blocks
order: 101
agent:
  intent: The credential card of an authentication screen, in its sign-in, sign-up or forgot-password mode.
  whenToUse: 'A page or panel where someone enters credentials: a sign-in screen, a registration screen (mode="sign-up"), a password-recovery screen (mode="forgot-password"), a re-authentication step, a modal that unlocks a privileged action.'
  whenNotToUse:
    - case: Collecting a one-time code sent by email or SMS
      use: PinInput, inside the verify screen
    - case: A full-viewport page with its own heading, highlights and footer
      use: the sign-in, sign-up or forgot-password screen, each of which composes this block in one mode
    - case: Setting the new password behind an emailed link
      use: the reset-password screen — this card's forgot-password mode only asks for the address
    - case: A long form of grouped fields with one actions row
      use: FormLayout — settings, profile, billing; this card is credentials only
    - case: A generic form of your own fields
      use: Field and Button directly
  gotchas:
    - "Presentational: it holds no credentials and issues no request. Pass `error` / `loading` / `disabled` and handle the native submit event yourself."
    - "`mode` decides which card this is: `sign-in` (the default), `sign-up` or `forgot-password`. It changes the fields, the copy, the secondary row and the footer link; everything else — width, spacing, states — is shared."
    - '`sent` is read in `forgot-password` only: the same card confirms in place instead of asking, names `sentTo` in the sentence and carries it in a hidden input, so "Send it again" is a plain form submission that works unhydrated. The confirmation says "if that address has an account" and never whether it does.'
    - "`error` is a form-level message. Signing in it also invalidates both credential fields and names neither, so the form never confirms that an email address exists; signing up there is nothing to leak yet, so `errors` (keyed by the field's `name`: `fullName`, `email`, `password`) names the field that was rejected."
    - '`errors` is read in `sign-up` only, and the terms links (`termsHref` / `privacyHref`) render there only — as does the required consent checkbox, which is `name="terms"` in the submitted form.'
    - "`loading` implies inert: every control is disabled and the submit carries aria-busy, so a second submit cannot be fired mid-request."
    - "The block responds to its container, never the viewport. Mount it in something narrower than --container-sm and the secondary row stacks on its own."
    - "`titleLevel` is the rank the card's title carries in the page's heading outline. It defaults to `3` — a card is a section of a page, not its heading — and a screen that mounts this card as the whole route passes `1`, so the document has a top-level heading and nothing after it skips a rank."
    - "In `forgot-password` the card's header is a live region, so the confirmation that rewrites it in place is announced and not only seen."
    - "It is a block, so you own the copy: the labels, the links and the hrefs are yours to edit after `moderno add`."
  theming:
    - "The surface is Card (--card, --border, --radius); the controls are Field and Checkbox; the submit is Button's primary variant. Nothing is painted here."
    - "The error alert reads --destructive through Alert's error variant, so a destructive action and a failed sign-in speak with one voice."
    - "Layout comes from the Tailwind preset's contract-backed utilities; the card's width cap is --container-sm."
---

import Preview from "../../../components/Preview.astro";
import Install from "../../../components/Install.astro";
import FrameworkSelect from "../../../components/FrameworkSelect.astro";
import LoginFormBlockDemo from "../../../islands/LoginFormBlockDemo.svelte";
import loginFormReact from "../../../../../../registry/blocks/login-form/react/login-form.tsx?raw";
import loginFormVue from "../../../../../../registry/blocks/login-form/vue/LoginForm.vue?raw";
import loginFormSolid from "../../../../../../registry/blocks/login-form/solid/login-form.tsx?raw";
import loginFormSvelte from "../../../../../../registry/blocks/login-form/svelte/LoginForm.svelte?raw";

**Login form** is the credential card, and it has three modes.
`mode="sign-in"` — the default — is the returning person: an email field, a
password field, remember-me, and the two links every sign-in screen needs.
`mode="sign-up"` is the new one: full name, email, a new password, and the
consent that has to be given before an account can exist. `mode="forgot-password"`
is the one who cannot get in: the address alone, and — once `sent` — the same
card confirming that a link is on its way. It composes `Card`, `Field`,
`Checkbox`, `Button` and `Alert` and paints nothing of its own, so a theme
re-skins it untouched.

One block rather than three, because they are one thing: the same card, the same
width cap, the same rhythm — so a person walking from [sign in](/en/sign-in/) to
[sign up](/en/sign-up/) to [forgot password](/en/forgot-password/) does not feel
the page change under them.

It is a _block_, not a screen: it does not own the viewport, holds no navigation
state and issues no request. The `sign-in`, `sign-up` and `forgot-password`
screens compose it and keep the state; this file is a section you can also drop
into a modal or a settings panel.

<FrameworkSelect locale="en" />

<Preview
  examples={{
    react: loginFormReact,
    vue: loginFormVue,
    solid: loginFormSolid,
    svelte: loginFormSvelte,
  }}
>
  <LoginFormBlockDemo client:visible />
</Preview>

## Installation

<Install item="login-form" />

Installing a block copies its file into your project and records it in
`.moderno/manifest.json` under its own version, so `moderno update` and
`moderno diff` keep working per item. From that moment the file is yours.

## Sizing

The block responds to **its container**, never the window. The card caps itself
at `--container-sm` (24rem) — a credential field wider than that is harder to
read, not easier — and the secondary row (remember-me plus the recovery link)
stacks below the `@sm` step and sits on one line at or above it. The heading
steps up at `@md`, where the section has room to breathe.

That is why the same file is correct in a 320px sidebar, inside a card and
centred on a 1280px page, in light and dark alike, without a single media query.
The rules behind this are in [Blocks and containers](/en/blocks-and-containers/)
and ADR-0005.

## Modes

`mode` changes what the card asks for and nothing else — the width cap, the
spacing, the container steps and every state below are shared.

| Mode                | Fields                                      | Secondary row                         | Footer link                        |
| ------------------- | ------------------------------------------- | ------------------------------------- | ---------------------------------- |
| `sign-in` (default) | Email, password (`current-password`)        | Remember me + "Forgot your password?" | "New here? Create an account"      |
| `sign-up`           | Full name, email, password (`new-password`) | Required consent box + legal links    | "Already have an account? Sign in" |
| `forgot-password`   | Email                                       | None                                  | "Remembered it? Sign in"           |

`forgot-password` has a second face rather than a fourth mode. With `sent`, the
same card keeps its place and rewrites itself: the header becomes "Check your
inbox", the field gives way to the sentence saying what was sent and for how long
it is good, and the submit becomes a secondary "Send it again" that resubmits
`sentTo` from a hidden input — so the resend is a plain form submission and works
on a page whose JavaScript never arrived. Nothing moves, because a card that
jumps at the moment of confirmation makes the reader find it again exactly when
they were told to go and look somewhere else.

The confirmation reads "if that address has an account" and renders identically
whether one exists or not. A recovery card that answers differently for a known
and an unknown address is an account-enumeration endpoint; the block will not be
one, and the code you wire behind it should not be either.

The card's title is an `h3`, because a card is a section of a page and not its
heading. A **screen** mounts this card _as_ the whole route, though, and there
`h3` is wrong twice over: the page has no top-level heading, and the `h2` the
aside prints next reads as an outline that skips a rank. So the card takes
`titleLevel`, and the three auth screens pass `1` — the title of the card is what
the page is called, so it is the page's `h1` rather than a second heading printed
above it. The default is `3`, so a card you mount yourself is unchanged.

Recovering, the card's header is also a live region. `sent` rewrites the title
and the description where they stand — that is the point — but a card whose whole
content changed silently leaves a screen reader user one clue that anything
happened: the button they just pressed renamed itself. The region is there from
the moment the card mounts, because a live region added at the same time as the
change is not announced.

The preview above is the `sign-in` mode in its container widths and states; the
`sign-up` mode is previewed on the [sign-up screen](/en/sign-up/) page and the
`forgot-password` mode, both faces, on the
[forgot-password screen](/en/forgot-password/) page — each mounted in the screen
that composes it.

## States

| State           | What renders                                                                                                                |
| --------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Default / empty | The pristine form — placeholders, no values. A sign-in form has no collection to be empty of, so these are the same render. |
| Hover           | The controls' own affordances from `components.css`; both links raise to `--foreground` and underline.                      |
| Focus visible   | The shared 2px `--ring` outline on every control, and on the two links.                                                     |
| Disabled        | `disabled` — sign-in is unavailable (an SSO-only workspace, a locked account).                                              |
| Loading         | `loading` — every control inert, the submit `aria-busy` with a spinner that stops under `prefers-reduced-motion`.           |
| Error           | `error="…"` — one form-level `Alert`; in `sign-in` both credential fields also go invalid.                                  |
| Field errors    | `errors={{ email: "…" }}` — `sign-up` and `forgot-password`: that field is invalid and prints its own message.              |
| Sent            | `sent` — `forgot-password` only: the card confirms in place, and the submit becomes "Send it again".                        |

Signing in, the error is deliberately form-level. Telling a visitor that _the
password_ was wrong also tells them the email exists, which is the first half of
a credential stuffing run; the block never does that, and neither should the code
you wire behind it. Signing up, that argument is gone — the address is being
offered, not guessed — and a form that will not say which field it rejected is
merely rude, so `errors` names them.

## Props

| Prop          | Type                                          | Default     | What it does                                                                                  |
| ------------- | --------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------- |
| `mode`        | `"sign-in" \| "sign-up" \| "forgot-password"` | `"sign-in"` | Which card this is: the returning person, the new one, or the locked-out one.                 |
| `titleLevel`  | `1 \| 2 \| 3`                                 | `3`         | The rank the card's title carries in the page's heading outline; a screen passes `1`.         |
| `sent`        | `boolean`                                     | `false`     | `forgot-password` — the link has gone out: the card confirms instead of asking.               |
| `sentTo`      | `string`                                      | `""`        | `forgot-password` — the address named in the confirmation and resubmitted by "Send it again". |
| `error`       | `string`                                      | —           | Form-level failure message; renders the alert, and in `sign-in` invalidates both fields.      |
| `errors`      | `Record<string, string>`                      | —           | `sign-up` / `forgot-password` — per-field messages keyed by the field's `name`.               |
| `loading`     | `boolean`                                     | `false`     | The submit is in flight: controls inert, button busy.                                         |
| `disabled`    | `boolean`                                     | `false`     | The form is unavailable (SSO-only workspace, locked account, closed beta).                    |
| `onSubmit`    | submit handler                                | —           | The native submit event. React/Solid take `onSubmit`, Svelte `onsubmit`, Vue emits `submit`.  |
| `forgotHref`  | `string`                                      | `"#"`       | `sign-in` — where "Forgot your password?" points.                                             |
| `signUpHref`  | `string`                                      | `"#"`       | `sign-in` — where "Create an account" points.                                                 |
| `signInHref`  | `string`                                      | `"#"`       | `sign-up` / `forgot-password` — where "Sign in" points.                                       |
| `termsHref`   | `string`                                      | `"#"`       | `sign-up` — where the terms link under the consent box points.                                |
| `privacyHref` | `string`                                      | `"#"`       | `sign-up` — where the privacy link under the consent box points.                              |
