/**
 * A Divider with a label — the rule splits around it — @moderno-ui/react.
 */
import { Divider } from "@moderno-ui/react";

export function DividerLabelDemo() {
  return (
    <Divider>Or continue with</Divider>
  );
}
